spring.datasource.url = ${DB_URL:jdbc:mysql://localhost:3306/music}?useUnicode=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Azazel123321}

spring.servlet.multipart.max-file-size=10000MB
spring.servlet.multipart.max-request-size=10000MB

server.url=${TS_HOLDING_SERVER_URL:http://localhost:8080}
compilation.command.word.path=${COMPILATION_COMMAND_PATH:ffmpeg}